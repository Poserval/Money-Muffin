{
  "name": "money-muffin",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p capacitor-web && cp *.html *.json *.js *.css capacitor-web/ && cp -r icons capacitor-web/",
    "clean": "rm -rf capacitor-web android",
    "cap:add": "npm run build && npx cap add android && npm run clean",
    "cap:sync": "npm run build && npx cap sync android && npm run clean"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/app": "^5.0.8",
    "@capacitor/splash-screen": "^5.0.8"
  }
}
