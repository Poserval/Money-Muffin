{
  "name": "money-muffin",
  "version": "1.0.0", 
  "scripts": {
    "build:capacitor": "mkdir -p capacitor-web && cp *.html *.json *.js *.css capacitor-web/ && cp -r icons capacitor-web/",
    "clean:capacitor": "rm -rf capacitor-web",
    "cap:add": "npm run build:capacitor && npx cap add android && npm run clean:capacitor",
    "cap:sync": "npm run build:capacitor && npx cap sync android && npm run clean:capacitor"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  }
}
